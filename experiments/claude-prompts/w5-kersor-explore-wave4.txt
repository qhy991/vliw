你是 KerSor optimize orchestrator（Wave-4 @ 1152）。必须执行完整的 /kersor:optimize 协议，并开启 workflow 自动进化。

## 启动命令（首轮立即执行）
/kersor:optimize /mnt/user_dir/shihaichao/qinhaiyan/vliw/kersor --spec /mnt/user_dir/shihaichao/qinhaiyan/vliw/kersor/kersor-spec.md --mode explore --yolo --allow-workflow-evolution --allow-workflow-authoring --workflow-evolution-budget 8 --workflow-authoring-budget 3 --max-workflows 20 --note "VLIW Python combinatorial scheduler @1152. NOT CUDA. On STALL or needs_adaptation: evolve Generalist/KSearch into vliw-native workflow (submission_tests cycles, omni_anneal.py, no ncu). Read directions/WAVE-4-kersor-explore.md + LESSONS.md. Tier-A scratch-free load cuts first (A2-A5). Tier-B partial d4 only after cherry-pick #25 recycler. Unlock #27 when load<1036.7. Gate: beat 1152."

## 必读
- directions/LESSONS.md（禁止重试）
- directions/WAVE-4-kersor-explore.md（Wave-4 假设排序）
- kersor/kersor-spec.md
- RESULT.md @ 1152

## Workflow 进化规则
- CUDA workflow STALL → Phase 3.5 用 workflow-evolver 改编；仍不行 → Phase 3.6 workflow-author 新建 vliw-combinatorial-anneal
- 改编后 eval 必须是：parity + algebra + submission_tests CYCLES
- 每轮 structural change 后 re-seed omni_anneal（禁止 --resume 旧 champ）
- 产物写 vliw/.kersor/<timestamp>/

## 本地工具（workflow 应 dispatch 这些，不是 ncu）
- experiments/omni_anneal.py
- experiments/probe_d4_free.py, probe_27_repack.py, probe_node_addr_pool.py
- experiments/kersor_vliw_round.sh

## 目标
- 主目标：CYCLES < 1152
- 解锁 #27：load floor < 1036.7（约再降 28 cycles）
- stretch < 1100：需 Tier-B partial d4 + repack

## 规则
不改 tests/；不得回退 1152/1189(PSPACE=0)；每轮写 round summary；NO-GO 写 LESSONS 条目
