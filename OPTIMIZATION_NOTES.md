# VLIW Kernel 优化复盘:1249 → 1230 cycles

> 对象:`perf_takehome.py` 中的 `KernelBuilder.build_kernel`
> 固定测试:`forest_height=10, rounds=16, batch_size=256`
> 结果:1249 → **1230 cycles**(118.28x → **120.11x**),正确性在无种子随机输入下验证通过,`tests/` 零改动。

这份文档记录的不是"改了哪几行",而是**怎么判断该往哪改、哪些方向是死路、以及为什么**。对这类"已经被高度优化过的 kernel 再抠几个 cycle"的任务,方法论比具体改动更有价值。

---

## 0. 一条铁律:先证伪,再相信

起点是一份"1249 已接近理论最优、建议放弃"的分析。它的每个论点听起来都合理,但**没有一个是在模拟器上实测过的**。第一步不是接受结论,而是把每个 load-bearing 的论断丢进模拟器验证。结果:核心前提就是错的(见 §2)。

**教训:对性能分析,"听起来对"和"实测是对的"之间隔着一整个优化空间。任何"理论下界""不可能"的论断,先量出来再说。**

---

## 1. 吃透机器模型:这台机器的三个关键事实

在动手前必须先确立三个事实,它们决定了整个搜索空间的形状:

### 1.1 cycle 数 == bundle 数,且与输入数据无关

`batch=256` → `K_VEC=32, n_groups=1`(单组,无循环),所以:

```
cycles == len(kb.instrs)   # 精确相等
```

而且 `build_kernel` 只吃 `(forest_height, n_nodes, batch_size, rounds)` 四个 **shape** 参数,**不吃输入值**。所以生成的程序对数据是确定的:

> **同一 shape 下 cycle 数跨 seed 完全确定;无种子的 submission 测试只改变"正确性覆盖",从不改变 cycle 数。**

这条极大加速了迭代:**改动只要不影响正确性,就可以只数 bundle,不必跑完整模拟。**

### 1.2 引擎槽位与"同一个 op 可以换引擎发射"

```
alu: 12 槽/cycle    valu: 6    load: 2    store: 2    flow: 1
```

一个向量 XOR(8 lane)有两种发射方式,**结果比特完全一样**:

| 方式 | 占用 |
|---|---|
| `v_alu("^", …)` | 1 个 valu 槽 |
| `v_alu_scalar("^", …)` | 8 个 alu 槽(每 lane 一个标量 op) |

> **"某个向量 op 放在 alu 还是 valu"是一个纯调度旋钮,永远不影响正确性。** 这是整个优化的地基。

### 1.3 引擎下界(实测 op 数 / 槽数)

```
alu:  14336 / 12 = 1195   ← 单引擎绑定下界
valu:  7009 /  6 = 1169
load:  2196 /  2 = 1098
flow:   736 /  1 =  736
```

---

## 2. 推翻错误前提:1195 不是硬底

那份分析说"下界 1195 由 ALU 决定,想突破必须减少 ALU op 数,但那是算法定义的、不能省"。

**错在哪:** 那 14336 个 ALU op **全部**来自 `v_alu_scalar` 这个"把 XOR 从 valu 挪到 alu"的刻意 rebalance(见 §1.2)。alu/valu 的划分是一个**可调旋钮**,不是硬约束。真正的下界是两个引擎的**合并容量**:

```
每 bundle 可做 = 6 valu-op + 12 alu-slot = 6 + 12/8 = 7.5 个"向量-op 等价"
扣掉必须在 valu 的 2016 个 multiply_add,剩 6785 个 flexible op
=> 合并下界 = ceil((6785 + 2016) / 7.5) ≈ 1174   （不是 1195！）
```

**所以"1249 距 1195 只有 4.5%、已到顶"的说法是伪命题——真正的 throughput 下界是 1174,当前距它 75 cycle。**

---

## 3. 系统性地排除死路(负结果同样是产出)

在花大力气之前,先用便宜的实验把"看起来有希望"的方向逐个证伪。**记录负结果,避免后人重走。**

| 方向 | 实测结果 | 结论 |
|---|---|---|
| **调度器配置网格**:`step∈{2..6} × rot∈0..31 × 5 种优先级 key`(640 组) | 全局最优 = `step=4, key=(-hgt,-succ,i), rot=29` → **1249** | 配置空间**已穷尽**,没有任何组合能更好 |
| **粗放全局 rebalance**(把前 N 个 combine 向量化) | N=16→1244 是唯一略好的,N≥24 全部变差 | 全局挪动会拖累"双满"的中段 |
| **depth 选择性 rebalance**(按 depth 决定) | 全部回退(1277–1348) | depth 0 含 round 11,落在中段 |
| **重新 gather 取代 depth-2/3 的 mux** | d3→1380,d2→1421,越多越差 | **mux 是对的**:它让 `node` 常驻,`val^node→hash` 递归链不必等 load 延迟 |
| **NUM_MTMP_GROUPS 扫描** | 2:1253 **3:1249** 4:1252 5:1250 6+:OOM | 现有的 3 已是最优 |
| **缩短 hash 依赖链 / 更深流水线**(多 agent 结构性尝试) | 均无改进 | hash 六级严格串行、代数不可约;中段联合饱和,重排 emission 无收益 |

**关键洞察来自逐 bundle 的引擎占用剖析:**

```
windup [0-100]:    alu 83% / valu 43%    ← valu 闲
middle [200-1000]: alu ~100% / valu ~100% ← 两个都满,无解
drain [1100-1249]: alu 14-94% / valu 2-45% ← valu 大量闲置
```

> 1249 个 bundle 中只有 **17 个是纯依赖 stall**——调度本身已近乎完美。75-cycle 的 gap **全部**落在头尾两段:那里在飞的 vector 太少,填不满两个引擎。

这一步把问题从"泛泛地找优化"收敛成一句精确的话:**只有头尾两段的 valu 空槽是可用资源。**

---

## 4. 真正奏效的两个改动

### 4.1 头尾定向 rebalance(主要贡献,约 −14)

既然中段两引擎都满、头尾 valu 空,那就**只在头尾**把 combine 从 alu 挪回 valu,中段保持在 alu:

```python
def _combine(self, dest, a, b):
    gi = self._combine_no; self._combine_no += 1
    if gi < self._combine_head or gi >= self._combine_total - self._combine_tail:
        self.v_alu("^", dest, a, b)          # 头尾:放 valu(填空闲)
    else:
        self.v_alu_scalar("^", dest, a, b)   # 中段:留 alu(别碰满载区)
```

- 用计数器按**发射顺序**判断 combine 落在哪一段;每个 rotation 在 `gen_body` 里 reset。
- 发射顺序与调度位置强相关(对角线 emission),所以"前 `head` + 后 `tail` 个"能精准命中 windup/drain。
- 头尾的绑定瓶颈是 alu,把 XOR 从 alu(8 槽)搬到空着的 valu(1 槽)=给 alu 松绑。
- `head=10 / tail=100` 由 **64 + 25 次完整 build 的网格扫描**定出。

### 4.2 depth-0 常数折叠(锦上添花)

depth 0(round 0、11)结构不变量保证每 lane `idx==0`,故 `2*idx+1 ≡ 1`:

```python
if depth == 0:
    self.v_alu("+", idx, one_v, addr)        # idx = 1 + rem,省掉一个 muladd
else:
    self.v_muladd(node, idx, m2, one_v)
    self.v_alu("+", idx, node, addr)
```

省一个 valu op × 32 vec × 2 round。单独作用不大(valu 非全局瓶颈),但与 §4.1 叠加后一起进了最优组合。

**一句话:没减少任何运算、没动算法——只是把"XOR 摆 alu 还是 valu"这个自由度从全局粗放改成按调度区间精细分配,让每段都吃满它当时空闲的那个引擎。**

---

## 5. 诚实的 caveat

- 头尾参数的响应面**有噪声**:邻近配置在 1230–1240 之间跳。`head=10/tail=100→1230` 是实测最优,邻居(10/96→1233、14/88→1232)佐证该区间确实好,但**最后几个 cycle 有调度器 packing 的运气成分**,不是稳健的结构性收益。
- 1230 是**完全可复现、正确性验证过**的。
- 深层结构改动(缩链、更深流水线)确实一无所获,所以 **1230 已接近此调度器下的实际极限**。原分析"1249 接近最优"在量级上大致对,但归因错了:瓶颈不是 ALU throughput,而是 windup/drain 的调度 gap,其中约 19 cycle 是可回收的。

---

## 6. 可迁移的原则

1. **先证伪再相信**:任何"理论下界""不可能"的论断,先在真实度量器上量出来。
2. **吃透成本模型里的"自由度"**:本题的核心是"同一 op 可换引擎且不改变结果"——找到这种**免费的、正确性无关的旋钮**,是抠 cycle 的第一入手点。
3. **剖析瓶颈的空间分布,而非只看总量**:总量层面"两引擎都接近满"会让人放弃;**分段**看才发现头尾有空槽。全局最优参数常常是分段策略。
4. **穷尽便宜的配置空间**(网格搜索)以确认"配置调优已到顶",这样才知道必须转向结构性改动。
5. **负结果要记录**:re-gather 变差、depth 选择性 rebalance 变差、缩链无收益——这些省下后人的时间,也反过来验证了现有设计的合理性。
6. **区分"真收益"和"packing 噪声"**:响应面有噪声时,用邻域佐证,并如实标注哪些 cycle 是运气。

---

## 7. 验证清单(提交前必做)

```bash
# 1. tests/ 必须与 origin/main 逐字节一致(反作弊)
git diff origin/main -- tests/          # 必须为空

# 2. 正确性 + cycle 数(无种子随机输入,跑 8 次正确性)
python tests/submission_tests.py        # 必须打印 OK,并给出 CYCLES: 1230
```

> ⚠️ Readme 明确警告:发布首日大量 <1300 的提交是**无效的**,因为模型改了测试。**永远不要碰 `tests/`(含 `frozen_problem.py`)。**
